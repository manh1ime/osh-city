ПРОМПТ — часть 2 (остаток работ по переводу кафе «Учкудук» в «Ош-Сити»)
========================================================================

Рабочая папка: D:\Osh-City  (это копия приложения — Next.js + Prisma + PostgreSQL)
Эталон:        D:\Uchkuduk\uchkuduk-original — НЕ ИЗМЕНЯТЬ, все правки только в D:\Osh-City

Кафе «Ош-Сити»: ОДИН филиал, адрес Ленинский проспект, 148, телефон +7(931)392-00-02.


УЖЕ СДЕЛАНО — ЭТО ПОВТОРЯТЬ НЕ НУЖНО
------------------------------------
- Название «Учкудук» заменено на «Ош-Сити» во всём проекте (код, конфиги, .env, документация).
- Все адреса филиалов удалены, оставлен один: Ленинский проспект, 148.
  Константы: src/lib/branches.ts (RESTAURANT_ADDRESS, RESTAURANT_PHONE, RESTAURANT_PHONE_HREF).
- Телефон в подвале сайта: +7(931)392-00-02.
- Меню полностью заменено на меню из D:\Меню Ош-Сити:
  prisma/menu-data.ts = 9 категорий / 67 позиций, 67 фото в public/menu.
  Старые блюда и старые фото удалены.
- В базе/сидах филиал ровно один (src/lib/branches.ts, prisma/seed-data.ts, prisma/seed.ts,
  prisma/replace-branding.sql). Лишние филиалы помечаются неактивными.


ЧТО НУЖНО СДЕЛАТЬ
-----------------

1) Убрать из приложения возможность выбора филиала и все упоминания «филиалов»
   во множественном числе. Раньше кафе было с двумя филиалами, теперь зал ровно один,
   поэтому:

   а) Убрать селекты/фильтры «Все филиалы»:
      - src/app/manager/orders/page.tsx        (параметр ?branch=, <select name="branch">)
      - src/app/manager/reports/page.tsx       (<select name="branch">)
      - src/app/manager/reservations/page.tsx  (<select name="branch">, текст «Все филиалы и
                                                предзаказы», <ReservationsBoard showBranch />)
      - src/components/manager/StaffManager.tsx (поле «Филиал» в карточке сотрудника,
                                                текст «Для официанта филиал обязателен»,
                                                «Оба филиала»)
      - src/components/staff/ReservationsBoard.tsx (колонка/подпись филиала, проп showBranch)

   б) Привести тексты к одному залу:
      - src/app/staff/layout.tsx               (в шапке «… · Все филиалы»)
      - src/app/staff/notifications/page.tsx   («Ваш филиал: все филиалы» / «по карточке
                                                сотрудника»)
      - src/app/staff/reservations/page.tsx    («Все филиалы», «Вам не назначен филиал…»)
      - src/app/manager/staff/page.tsx         (ensureBranches, привязка сотрудника к филиалу)
      - src/app/manager/reservations/page.tsx, src/app/manager/orders/page.tsx — подписи

   в) Проверить служебную фильтрацию по branchId (src/app/staff/calls/page.tsx,
      src/app/staff/orders/page.tsx, src/app/staff/orders/[orderId]/page.tsx):
      она должна молча работать с единственным филиалом, без выбора в интерфейсе.

   Важно: модель Branch в схеме Prisma и привязки (branchId у столов, броней, сотрудников)
   НЕ удалять — на них ссылаются заказы и история. Убирается только выбор и упоминания
   в интерфейсе.

   г) После правок — прогнать поиск и убедиться, что в src/ не осталось
      «Все филиалы», «Оба филиала», «выберите филиал», «двух филиалах», «все филиалы».

2) Проверить сборку:
   - npx tsc --noEmit — без ошибок;
   - npm run build — успешно.

3) Цены и составы блюд — НЕ трогать, я загружу данные отдельно.
   Сейчас в prisma/menu-data.ts у всех 67 позиций price = 0 и пустое описание.
   Список позиций для заполнения — в PRICE_TODO.md.
   Когда я пришлю цены и составы: внеси их в prisma/menu-data.ts
   (поле price — целое число в рублях, 4-й аргумент dish(...) — описание/состав),
   затем примени: npx tsx prisma/replace-menu.ts.


ОТЧЁТ
-----
Когда закончишь п.1 и п.2, допиши в D:\Osh-City\PROGRESS_REPORT_PART1.txt
(или создай PART2) коротко: какие файлы изменил и что осталось.
