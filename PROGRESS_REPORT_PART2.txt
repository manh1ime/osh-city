ОТЧЁТ: Убран выбор филиала из интерфейса. Вторая часть работ
=============================================================

Дата: 2026-09-06
Рабочая папка: D:\Osh-City  (полная копия проекта)
Оригинал:      D:\Uchkuduk\uchkuduk-original  — НЕ ТРОГАЛ, все изменения только в D:\Osh-City


1. ЧТО СДЕЛАНО В ЭТОМ ЗАХОДЕ
-----------------------------

Убрана возможность выбора филиала и все упоминания «филиалов» во множественном
числе из интерфейса. Теперь в приложении ровно один зал — Ленинский проспект, 148.
Модель Branch в схеме Prisma и привязки (branchId у столов, броней, сотрудников)
СОХРАНЕНЫ — на них ссылаются заказы и история. Убран только выбор в интерфейсе.

А) Убраны селекты/фильтры выбора филиала
   - StaffManager.tsx — убрано поле «Филиал» из карточки сотрудника,
     колонка «Филиал» из таблицы, текст «Для официанта филиал обязателен»,
     текст «управляет обоими кафе». Проп `branches` и тип `StaffBranchOption`
     удалены из компонента.
   - manager/staff/page.tsx — убран импорт `ensureBranches`, запрос branches
     из БД, передача `branches` и `branchId` в компонент.
   - TableManager.tsx — селект филиала заменён на hidden-поле (единственный
     филиал назначается автоматически). Убрано отображение branchName/branchAddress
     в карточке стола.
   - ReservationsBoard.tsx — проп `showBranch` отсутствовал (уже был убран ранее),
     колонки филиала в доске броней нет.

Б) Приведены тексты к одному залу
   - staff/layout.tsx — убрано «Все филиалы» из шапки (бралось из branch.name).
     Теперь показывает только имя, роль и смену.
   - staff/notifications/page.tsx — «Ваш филиал: все филиалы» заменено на
     «Зал: Ош-Сити · Ленинский проспект, 148».
   - staff/reservations/page.tsx — убрано «Все филиалы», «Вам не назначен
     филиал», проп `showBranch`, поле `branchName` из DTO.
     Подзаголовок заменён на «Ош-Сити · Ленинский проспект, 148».
   - reservation/[code]/page.tsx — подпись «Филиал» заменена на «Адрес»
     (гость видит адрес, а не внутреннее слово «филиал»).

В) Серверные действия и комментарии
   - actions/staff.ts — убрана валидация «Выберите филиал для официанта».
     Теперь официанту автоматически назначается единственный активный филиал
     через getPrimaryBranchId().
   - lib/notifications.ts — комментарий «видит оба филиала» -> «видит все столы».
   - lib/push.ts — «по обоим филиалам» -> «целиком», «ведёт оба филиала» ->
     «видит все столы».
   - actions/reservations.ts — «менеджер видит оба» -> «видит весь ресторан».

Г) Служебная фильтрация по branchId оставлена работать молча:
   - staff/calls/page.tsx, staff/orders/page.tsx, staff/orders/[orderId]/page.tsx
     — фильтр по branchId для не-менеджера сохранён, но в интерфейсе
     не показывается.
   - staff/reservations/page.tsx — фильтр по branchId для не-менеджера
     сохранён в запросе, но в интерфейсе не виден.

Д) OrdersBoard.tsx — убрано отображение branchName в карточке заказа
   (избыточно, когда филиал один).


2. ПРОВЕРКА
-----------

Поиск по src/: «Все филиалы», «Оба филиала», «выберите филиал», «двух филиалов» —
совпадений НЕТ.

   npx tsc --noEmit — ошибок нет.
   npm run build    — успешно (compiled, lint, static pages — OK).


3. ФАЙЛЫ, ИЗМЕНЁННЫЕ В ЭТОМ ЗАХОДЕ
-----------------------------------

src/components/manager/StaffManager.tsx    — убраны: проп branches, тип
  StaffBranchOption, поле branchId в StaffRow, колонка «Филиал» в таблице,
  селект филиала в форме, тексты «Для официанта филиал обязателен»,
  «Оба филиала», «управляет обоими кафе»
src/app/manager/staff/page.tsx            — убраны: импорт ensureBranches,
  запрос branches, передача branches/branchId в StaffManager
src/components/manager/TableManager.tsx    — селект филиала заменён на hidden,
  убрано отображение branchName/branchAddress в карточке стола
src/components/staff/OrdersBoard.tsx       — убрано отображение branchName
  в карточке заказа
src/app/staff/layout.tsx                   — убрано «Все филиалы» из шапки,
  убран select branch из запроса currentStaff
src/app/staff/notifications/page.tsx       — «Ваш филиал: все филиалы» ->
  «Зал: Ош-Сити · Ленинский проспект, 148»
src/app/staff/reservations/page.tsx       — убраны «Все филиалы», «Вам не
  назначен филиал», showBranch, branchName в DTO; подзаголовок -> адрес кафе
src/app/reservation/[code]/page.tsx       — подпись «Филиал» -> «Адрес»
src/actions/staff.ts                       — убрана валидация «Выберите
  филиал для официанта», добавлена автоматическая привязка через
  getPrimaryBranchId()
src/lib/notifications.ts                   — комментарий «оба филиала» ->
  «все столы»
src/lib/push.ts                            — комментарии «оба филиала» ->
  «все столы», «по обоим филиалам» -> «целиком»
src/actions/reservations.ts                — комментарий «видит оба» ->
  «видит весь ресторан»


4. ЧТО ОСТАЛОСЬ
---------------

1) Цены и составы блюд — ждём данные от вас. Список позиций в PRICE_TODO.md.
   Когда пришлёте: внесите в prisma/menu-data.ts (price — целое в рублях,
   4-й аргумент dish(...) — описание/состав), затем npx tsx prisma/replace-menu.ts.

2) Больше никаких правок по филиалам не требуется — выбор убран, тексты
   приведены к одному залу, сборка проходит.
